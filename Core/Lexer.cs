/*
    TODO: RegexTokenizedBuffer to store (ctx, index, length)
          On error report, refer to RegexParsed[0].ctx, index, length etc...
*/

namespace uhla.Core {
    internal static partial class Core {
        internal static Terminal.ErrorContext? Lex(object src) {
            return null;
        }
    }
}
